#include "stdio.h"
int main()
{
  int sum=0,i,n; 
  scanf("%d",&n);
  for(i=1;i<n;i++)
  {
    if(i%3==0)
    {
      sum+=i;
      continue;
    }
    if(i%5==0)
    {
      sum+=i;
      continue;
    }
  }
  printf("%d",sum);
  return 0;
}
